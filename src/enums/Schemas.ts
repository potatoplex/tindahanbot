enum Schemas {
  COOLDOWNS = "wokcommands-cooldowns",
  DISABLED_COMMANDS = "wokcommands-disabled-commands",
  LANGUAGES = "wokcommands-languages",
  PREFIXES = "wokcommands-prefixes",
  REQUIRED_ROLES = "wokcommands-required-roles",
  CHANNEL_COMMANDS = "wokcommands-channel-commands",
  SLASH_COMMANDS = "wokcommands-slash-commands",
  TOPICS = "topics",
}

export default Schemas;
